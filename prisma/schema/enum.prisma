enum UserRole {
  customer
  admin
  merchant
}

enum PaymentMode {
  stripe
  razorpay
  cash
}

enum OrderCancellationEntity {
  merchant
  user
}

enum TokenType {
  refresh
  access
  verify_email
}
