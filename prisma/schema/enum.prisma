enum UserRole {
  customer
  admin
  merchant
}

enum PaymentMode {
  stripe
  razorpay
  cash
}

enum OrderCancellationEntity {
  merchant
  user
}

enum TokenTypes {
  refresh
  access
  // verify_email
}
